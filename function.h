#pragma once
#pragma once
#include <time.h>
#include <vector>
#include "person.h"
void readFile(vector<Person>& person);
void Show(const Person  worksheet[][5][3], const std::vector<Person> person);
void WorksheetAll(Person  worksheet[][5][3]);